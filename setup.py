import setuptools

setuptools.setup(
    include_package_data=False,
    name='airline-deps',
    version='1.0',
    packages=setuptools.find_packages(),
    description='All dependency files module.'
)
